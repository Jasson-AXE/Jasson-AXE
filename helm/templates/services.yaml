apiVersion: v1
kind: Service
metadata:
  name: my-web-service
  labels:
    env : prod
    owner: JohnDoe
spec:
  selector:
    project: web-app
  ports:
    - name      : app-listener
      protocol  : TCP
      port      : 80
      targetPort: 80
  type: LoadBalancer